apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: metacoding
spec:
  type: ClusterIP
  selector:
    app: kafka
  ports:
    - port: 9092
      name: broker
    - port: 9093
      name: controller

